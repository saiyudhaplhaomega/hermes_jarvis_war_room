"""jarvis_company_os — Jarvis spec 04 company OS (Phase 1 spine).

Implements: Boss D-A (apply via lifespan, schema_migrations tracking),
Boss D-B (lives inside dashboard backend),
Boss D-C (seed_default_company() called from startup, idempotent).
"""
__version__ = "0.1.0-phase1"
